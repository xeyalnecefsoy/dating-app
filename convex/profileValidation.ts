/** Re-export for Convex bundler — keep in sync with lib/profileValidation.ts */
export {
  BIO_MIN_LENGTH,
  BIO_MIN_WORDS,
  isTitleCaseWord,
  validateFullName,
  countWords,
  validateBio,
  validateProfileForSave,
} from "../lib/profileValidation";
